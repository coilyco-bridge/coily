binary: aws
mode: allow
# Only these top-level AWS services are in scope for coily. Everything else
# (eks, lambda, iam write ops, resource-explorer, etc.) stays out — they're
# either exploratory or handled by GitHub Actions.
roots:
  - route53
  - s3
  - s3api
  - ssm
  - sts
